package com.whtech.anuroopjodi.Constant;

/**
 * @author Pratik Butani on 23/4/16.
 */
public interface ApiService {


/*    @Multipart
    @POST(IUrls.URL_ADD_VENDOR)
    Call<ResponseBody> POSTAddVendor(@Part("user_id") RequestBody user_id,
                                     @Part("shop_name") RequestBody shop_name,
                                     @Part("vendor_type") RequestBody vendor_type,
                                     @Part("vendor_category") RequestBody vendor_category,
                                     @Part("vendor_name") RequestBody vendor_name,
                                     @Part("mobile_number") RequestBody mobile_number,
                                     @Part("phone_number") RequestBody phone_number,
                                     @Part("pan_number") RequestBody pan_number,
                                     @Part("gst_number") RequestBody gst_number,
                                     @Part("gst_registered") RequestBody gst_registered,
                                     @Part("gst_register_type") RequestBody gst_register_type,
                                     @Part("beat_id") RequestBody beat_id,
                                     @Part("zone_id") RequestBody zone_id,
                                     @Part("region_id") RequestBody region_id,
                                     @Part("address") RequestBody address,
                                     @Part MultipartBody.Part signaturefile,
                                     @Part("fssai_number") RequestBody fssai_number,
                                     @Part("discount_on_order") RequestBody discount_on_order);*/


    /*@Multipart
    @POST(IUrls.URL_UPDATE_PROFILE)
    Call<ResponseBody> POSTUpdateProfile(@Part("user_id") RequestBody user_id,
                                         @Part("first_name") RequestBody first_name,
                                         @Part("last_name") RequestBody last_name,
                                         @Part("email") RequestBody email,
                                         @Part MultipartBody.Part file);*/


  /*  ,
    @Part MultipartBody.Part file*/


/*    @Multipart
    @POST(IUrls.URL_ADD_TASK)
    public Call<ResponseBody> PostAddExpense(
            @Part("client_work_req_id") RequestBody client_id,
            @Part("ass_pack_sch_id") RequestBody schedule_id,
            @Part("app_user_id") RequestBody operator_id,
            @Part("visit_type_id") RequestBody visit_type_id,
            @Part("coordinate_person_name") RequestBody coordinate_person_name,
            @Part("coordinate_person_contact") RequestBody coordinate_person_contact,
            @Part("remarks") RequestBody remarks,
            @Part MultipartBody.Part signaturefile,
            @Part("work_charges") RequestBody work_charges,
            @Part("payment_mode") RequestBody payment_mode,
            @Part("cheque_number") RequestBody cheque_number,
            @Part("paid_amount") RequestBody paid_amount,
            @Part List<MultipartBody.Part> RoomPartList);





    @Multipart
    @POST(IUrls.URL_INSERT_MATERIAL)
    Call<ResponseBody> PostMaterialDelivery(
            @Part("client_work_req_id") RequestBody client_id,
            @Part("ass_pack_sch_id") RequestBody schedule_id,
            @Part("app_user_id") RequestBody operator_id,
            @Part("coordinate_person_name") RequestBody visit_type_id,
            @Part("coordinate_person_contact") RequestBody coordinate_person_name,
            @Part("remarks") RequestBody coordinate_person_contact,
            @Part MultipartBody.Part signaturefile,
            @Part("work_charges") RequestBody work_charges,
            @Part("payment_mode") RequestBody payment_mode,
            @Part("cheque_number") RequestBody cheque_number,
            @Part("paid_amount") RequestBody paid_amount);*/


    //@Part MultipartBody.Part[] multiplefiles

    /*@Part List<MultipartBody.Part> RoomPartList,
    @Part @Nullable MultipartBody.Part file*/











    /*



     */
//


  /*  @Multipart
    @POST("smart_steps/update_IDcard.php")
    Call<ResponseBody> getUpdateStationaryStudentIDCardAndProgressCardForm(
            @Part("user_id") RequestBody user_id,
            @Part("stationary_type_id") RequestBody stationary_type_id,
            @Part("ip_id") RequestBody ip_id,
            @Part("student_name") RequestBody student_name,
            @Part("student_group") RequestBody student_group,
            @Part("student_rollno") RequestBody student_rollno,
            @Part("student_dob") RequestBody student_dob,
            @Part("gender") RequestBody gender,
            @Part("academic_year") RequestBody academic_year,
            @Part("pincode") RequestBody pincode,
            @Part("address") RequestBody address,
            @Part("parent_name") RequestBody parent_name,
            @Part("parent_contact_no") RequestBody parent_contact_no,
            @Part MultipartBody.Part file


    );

    @Multipart
    @POST("smart_steps/update_IDcard.php")
    Call<ResponseBody> getUpdateWithoutImageStationaryStudentIDCardAndProgressCardForm(
            @Part("user_id") RequestBody user_id,
            @Part("stationary_type_id") RequestBody stationary_type_id,
            @Part("ip_id") RequestBody ip_id,
            @Part("student_name") RequestBody student_name,
            @Part("student_group") RequestBody student_group,
            @Part("student_rollno") RequestBody student_rollno,
            @Part("student_dob") RequestBody student_dob,
            @Part("gender") RequestBody gender,
            @Part("academic_year") RequestBody academic_year,
            @Part("pincode") RequestBody pincode,
            @Part("address") RequestBody address,
            @Part("parent_name") RequestBody parent_name,
            @Part("parent_contact_no") RequestBody parent_contact_no


    );*/


}
